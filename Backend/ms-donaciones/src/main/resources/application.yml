server:
  port: 8085

spring:
  application:
    name: ms-donaciones
  datasource:
    url: jdbc:postgresql://sanos_y_salvos_db:5432/sanosysalvos_db
    username: agustin
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true